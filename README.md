# Perseus Art & Architecture Browser

This version of the Perseus Art & Archaeology Browser is built using [CollectionBuilder](https://collectionbuilder.github.io/), a static-site generator based on Jekyll.  The data used to build the site is a large CSV file, currently generated from an AirTable base.
